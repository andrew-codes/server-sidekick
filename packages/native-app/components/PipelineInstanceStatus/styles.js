import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 2,
    backgroundColor: '#fff',
    flexDirection: 'row',
    height: 6,

  },
  item: {
    height: 6,
  }
});
