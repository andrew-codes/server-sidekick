import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  piList: {
    flex: 1,
    backgroundColor: '#fff',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
