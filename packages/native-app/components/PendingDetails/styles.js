import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  pending: {
    marginTop: 15,
  },
  pendingDetails: {
    marginLeft: 10,
  },
  buttonContainer: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  reasonBox: {
    height: 100,
    borderColor: 'gray',
    borderWidth: 1,
    marginTop: 60,
  }
});
